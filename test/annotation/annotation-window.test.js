import { expect } from 'chai';

import AnnotationWindow from '../../src/js/annotation/annotation-window';

describe('AnnotationWindow', function() {
  let cache = null;

  beforeEach(() => {
  });

  xit('something', () => {
    const annoWin = new AnnotationWindow({
    });
    annoWin.init();
  });
});
